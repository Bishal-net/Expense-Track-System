spring.application.name=ExpensesTrackerapp
server.port=8083

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/expensestracker?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=bishal2006
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Schema initialization
spring.jpa.defer-datasource-initialization=true

# allow devtools restart support
# spring.devtools.restart.enabled=true